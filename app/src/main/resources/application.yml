spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_db
    username: todouser
    password: todopass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true